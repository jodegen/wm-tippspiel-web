# Production-Defaults — von `next build` und `next start` automatisch geladen.
# NEXT_PUBLIC_* ist öffentlich (wird in den Client gebündelt); die API ist public.
# Daher unbedenklich eingecheckt und muss NICHT mehr je Befehl gesetzt werden.
NEXT_PUBLIC_API_BASE_URL=https://api.wm.xenoria.de/api/public
