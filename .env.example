# Vorlage für LOKALE Entwicklung: nach .env.local kopieren.
# In Produktion liefert die eingecheckte .env.production diesen Wert automatisch.
# Basis-URL der öffentlichen WM-Tippspiel-Backend-API (nur GET, inkl. /api/public,
# ohne abschließenden Slash).
NEXT_PUBLIC_API_BASE_URL=https://api.wm.xenoria.de/api/public
